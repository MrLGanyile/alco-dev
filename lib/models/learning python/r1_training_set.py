class R1TrainingSet:
   x_train: list # Column Vector
   y_train: list # Column Vector

   # Return a list with two elements [a, b]
   def find_ith_training_sample(self, i):
      return
   
   # Returns feature variable [list].
   def get_feature_variables(self):
      return
   
   # Returns target variable [list]
   def get_target_variables(self):
      return
   
   # Returns m [int]
   def get_no_of_samples(self):
      return

